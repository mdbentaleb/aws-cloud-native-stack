# ------------------------------------------------------------------------------
# ⚠️ REFERENCE ONLY: This file is NOT used directly by the live container.
# 
# Purpose: This configuration is uploaded to our AWS S3 Bucket.
# During deployment, 'setup.sh' pulls this file to verify IAM Role permissions.
# 
# Note: Using port 443 as we have SSL/TLS enabled for secure HTTPS traffic.
# ------------------------------------------------------------------------------

# server {
#     listen 443 ssl; # Using secure port
#
#     location / {
#         proxy_pass http://web-app:5000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
#
#     location /health {
#         access_log off;
#         return 200 'OK';
#     }
# }